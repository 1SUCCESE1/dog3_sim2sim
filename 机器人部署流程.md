# D1H 机器人部署完整流程

> 适用：将 `~/work/ddt_ros2_control` 开发工作区部署到一台全新的 D1H 机器人（arm64 / Ubuntu 22.04 / ROS2 Humble）。基于 2026-08-16/17 实测成功流程整理。

---

## 一、前置条件

| 项 | 说明 |
|---|---|
| 机器人系统 | Ubuntu 22.04（arm64），ROS2 Humble |
| 官方预装 | `/opt/d1_ros2`（无需卸载，部署时停服务即可） |
| 开发机 | x86 主机，工作区 `~/work/ddt_ros2_control` |
| 机器人连接 | `ssh robot@<机器人IP>`（密码 ddt） |

> ⚠️ 机器人 Python 为精简版：`colcon` 可能缺失、`coloredlogs` 可能损坏。
> ⚠️ **先 `hostname -I` 确认机器人 IP**，避免 scp 到错误主机。

---

## 二、完整流程

### 第 1 步：本机打包源码（排除构建产物）

```bash
cd ~/work/ddt_ros2_control
tar czf /tmp/ws.tar.gz \
  --exclude='.git' --exclude='build' --exclude='install' --exclude='log' \
  --exclude='onnxruntime-linux-x64-1.10.0' \
  controller hardware interaction simulation urdfs ros_utils

# onnxruntime 头文件单独打（跨架构，编译需要）
tar czf /tmp/onnxinc.tar.gz -C ~/work/ddt_ros2_control/onnxruntime-linux-x64-1.10.0 include

scp /tmp/ws.tar.gz /tmp/onnxinc.tar.gz robot@<机器人IP>:/home/robot/
```

**为什么用 tar 而非 scp 整个目录**：
- scp 整目录会把 `.git` / `build` / `install` 一起带过去
- `.git` 触发权限拒绝会中断传输；`build` 里的 CMakeCache 记录了开发机路径，到机器人上路径不一致导致 CMake 报错
- 只传源码最干净

### 第 2 步：机器人解压

```bash
mkdir -p ~/ddt_ros2_control/onnxruntime-linux-x64-1.10.0
tar xzf ~/ws.tar.gz -C ~/ddt_ros2_control
tar xzf ~/onnxinc.tar.gz -C ~/ddt_ros2_control/onnxruntime-linux-x64-1.10.0

# 确认源码与头文件到位
ls ~/ddt_ros2_control/controller/rl_controller/src/fsm/FSMState_RL.cpp
find ~/ddt_ros2_control/onnxruntime-linux-x64-1.10.0 -name onnxruntime_cxx_api.h
```

### 第 3 步：补齐编译依赖（精简系统）

```bash
# 安装 colcon
sudo apt install -y python3-colcon-common-extensions

# 若 colcon 报 coloredlogs 错误：
#   AttributeError: module 'coloredlogs' has no attribute 'install'
sudo /usr/bin/python3 -m pip install --force-reinstall --no-cache-dir coloredlogs
/usr/bin/python3 -c "import coloredlogs; print(hasattr(coloredlogs,'install'))"  # 应输出 True
```

### 第 4 步：编译

```bash
source /opt/ros/humble/setup.bash        # 必须，否则 colcon 不在 PATH
cd ~/ddt_ros2_control
export CPLUS_INCLUDE_PATH=$PWD/onnxruntime-linux-x64-1.10.0/include
export LIBRARY_PATH=/usr/lib             # onnxruntime 库用系统 aarch64 版

colcon build --symlink-install --packages-up-to \
  hardware_bridge d1_description rl_controller teleop_command keyboard_controller

source install/setup.bash
ros2 pkg prefix rl_controller            # ★ 必须输出 /home/robot/ddt_ros2_control/install/rl_controller
```

> **onnxruntime 关键点**：机器人 onnxruntime 库在系统路径 `/usr/lib/libonnxruntime.so`（aarch64，官方预置）。开发机 vendored 版是 x86，**不能用**，只需传它的 `include` 头文件（跨架构通用）。链接用 `LIBRARY_PATH=/usr/lib`，头文件用 `CPLUS_INCLUDE_PATH` 指向 vendored include。

### 第 5 步：停官方系统 + 启动

```bash
systemctl stop d1_bringup
# systemctl stop crsf_joy    # 如果有遥控接收节点
ros2 node list               # 确认无 /d1504xxxx 残留节点

ros2 launch rl_controller hw.launch.py robot:=d1h
```

> **⚠️ 必须 `robot:=d1h`，不是 `robot:=d1`**：模型是 8 关节（D1H），launch 的 `robot_name` 决定加载 `config/<robot>/controllers.yaml`。用 `d1` 会读 16 关节配置，模型不匹配。

启动后确认日志 5 个策略加载：

```
Get policy: getup.onnx in "transform_up_rl"
Get policy: flat.onnx in "rl_flat"
Get policy: squatdown.onnx in "rl_squatdown"
Get policy: instinct_flat_19.onnx in "instinct_flat_19"
Get policy: instinct_flat_24.onnx in "instinct_flat_24"
```

### 第 6 步：键盘控制（另开终端）

```bash
cd ~/ddt_ros2_control
source install/setup.bash
ros2 run keyboard_controller keyboard_controller_node
```

键位：`7` PD 起身 → `4` instinct_flat_24（最新策略）→ `w/s` 前进/后退 → `a/d` 转向。

---

## 三、踩坑汇总（务必规避）

| # | 坑 | 规避 |
|---|---|---|
| 1 | scp 错机器 | 先 `hostname -I` 确认 IP（本次 10.2.10.164 ≠ 实际 10.2.10.103） |
| 2 | scp 整个工作区 | tar 排除 .git/build/install/log/onnxruntime，避免 CMakeCache 路径错乱、.git 权限拒绝 |
| 3 | onnxruntime 架构 | 头文件从开发机传（跨架构），库用系统 /usr/lib 的 aarch64 版 |
| 4 | colcon 缺失 | `sudo apt install python3-colcon-common-extensions` |
| 5 | coloredlogs 损坏 | 强制重装，验证 `hasattr(coloredlogs,'install')` |
| 6 | 加载了官方包 | `ros2 pkg prefix rl_controller` 确认指向自己工作区 |
| 7 | robot:=d1 错误 | 8 关节模型必须 `robot:=d1h` |
| 8 | 官方系统抢控制 | 每次部署先 `systemctl stop d1_bringup` |

---

## 四、策略控制核心逻辑（背景）

- **policy_type**：yaml 的 `policy_type: "np3o"` 决定 FSM 类。`np3o` → base `FSMState_RL`（scaled obs + 双输入）。单输入模型（instinct_flat_19/24）靠 FSM 代码单输入补丁（`getInputShapes().size() > 1`）适配。
- **观测**：InstinctLab 训练是 scaled obs（ObsTerm scale=0.25 / 2.0 / 0.05），归一化已烘焙进 ONNX（Sub→Add→Div），控制器只喂 scaled obs。
- **轮子控制**：真机电机不支持 P_V 速度模式，已实现软件速度闭环 `tau = joint_kd × (目标轮速 − 实际轮速)`，用纯力矩下发，匹配训练端 Implicit 执行器（stiffness=0, damping）。